{{/* Risks stage：识别值得关注的风险，强制 JSON 输出 */ -}}
你是一位资深 code reviewer。从以下 PR 改动中识别**值得 reviewer 关注**的风险。

## PR 元信息
{{ .L1Meta }}
{{ with .L3Conventions }}
## 项目约定
{{ . }}
{{ end }}
{{ with .L2Files }}
## 改动文件
{{ range . -}}
### {{ .Path }}
```
{{ .Patch }}
```

{{ end -}}
{{ end }}
## 任务
仅输出**严格 JSON**（不要任何额外文字、不要 Markdown 代码块），结构如下：

```json
{
  "risks": [
    {
      "file": "path/to/file",
      "line": 123,
      "severity": "high",
      "category": "bug",
      "confidence": 0.95,
      "reason": "中文说明，≤ 80 字"
    }
  ]
}
```

字段约束：
- `severity`: `high` | `medium` | `low`
- `category`: `bug` | `security` | `perf` | `style` | `other`
- `confidence`: 0.0-1.0 浮点，你对此判断的把握度（前端 ≥ 0.9 默认展开）
- `reason`: 中文，≤ 80 字，聚焦"为什么是风险"
- `line` 可选，不确定时省略

判定原则：
- **high**：可能崩溃 / 数据丢失 / 安全漏洞 / 严重性能退化
- **medium**：会引发 bug 或维护成本
- **low**：风格 / 可读性 / 次要建议
- **宁可不报，不要乱报**：无明显风险时输出 `{"risks": []}`
