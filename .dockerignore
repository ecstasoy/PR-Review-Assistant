# Docker 构建上下文 = repo root（backend/Dockerfile 用）
# 目的：indexrepo 启动时要扫全仓做 RAG 预索引；context = root 才能 COPY backend + frontend + docs
# 大型 / 私有 / 构建产物全部排除：减小镜像体积 + 避免泄露 .env / .claude
#
# 注意：pattern 不带 `**/` 时只匹配根；node_modules 在 frontend/ 下要用 **/node_modules

# 版本控制 / 元数据
.git
.github
.DS_Store

# IDE / 工具私有
.idea
.vscode
.claude
.vercel
.cache
.turbo

# 依赖目录（任意深度都要排）
**/node_modules
**/__pycache__
**/.pytest_cache
**/.venv
**/venv

# 构建产物（任意深度）
**/.next
**/dist
**/build
**/out
**/target

# 数据 / 二进制产物（包含 SQLite 文件）
**/data
backend/server
backend/indexrepo
**/*.db
**/*.db-journal
**/*.db-wal
**/*.db-shm
**/*.log

# 本地秘钥
.env
.env.*
!.env.example
**/.env
**/.env.*

# 临时
**/*.tmp
